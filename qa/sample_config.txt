#location for incorta home.
#Default: /home/Incorta
incorta_home=/home/users/Incorta/

#path to tenant home
#Default: /home/tenants
tenant_home=/home/users/Incorta/tenants

#name of the tenant
#Default: Demo
tenant=demo

#Connection Url to incorta instance (in most cases localhost)
#Default: http://localhost:8080/incorta
url=http://localhost:8080/incorta

# Admin user username
#Default: Super
admin_user=super

#password for admin user login
#Default: incorta
admin_pass=incorta

#Flag to skip data / json validation. (options: False / True)
#Default: False
skip_validation=False

#Flag to skip xml meta data validation. (options: False / True)
#Default: False
skip_xml_validation=False

#Working Directory Path, Where all tests become timestamped and stored
#Default: ~/IncortaTesting/tmp/work
wd_path=/home/user/testing

#LDAP SETUP PROPERTIES (After first = symbol place connection info per ldap property)
#Default: environment dev 01 with port 389
ldap_url=ldap://dev01.incorta.com:389
ldap_base=dc=dev01,dc=incorta,dc=com
ldap_user_mapping_login=uid
ldap_group_mapping_member=member
ldap_group_search_filter=(objectClass=groupOfNames)

#Flag to include loading and validation of schemas? (useful if user only wants to test dashboards)
#Default: True
include_schemas=True

#Flag to run data validation stage with different users, tests permissions and security. (will generate NF diffs if set to False)
#Default: True
include_user_testing=True

#Flag to skip schema loading phase (options: False / True)
#Default: False
skip_data_load=False

#Indicator of what Test Suites / Test Cases to run
#Default is set to ALL, which will run all test suites
run_tests=ALL